# Routes @devitinternational packages to GitHub Package Registry.
# Set GITHUB_TOKEN in your environment to a token with read:packages scope.
# Generate one at: GitHub → Settings → Developer settings → Personal access tokens
@devitinternational:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
